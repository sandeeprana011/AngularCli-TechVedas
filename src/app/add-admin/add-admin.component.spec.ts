/* tslint:disable:no-unused-variable */

import {AddAdminComponent} from "./add-admin.component";

describe('Component: AddAdmin', () => {
  it('should create an instance', () => {
    let component = new AddAdminComponent();
    expect(component).toBeTruthy();
  });
});
